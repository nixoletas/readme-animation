services:
  web:
    image: nixoletas/welcome:latest
    container_name: welcome-nick
    ports:
      - "80:80"
    restart: unless-stopped
    environment:
      - NGINX_HOST=localhost
      - NGINX_PORT=80
